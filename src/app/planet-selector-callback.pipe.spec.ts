import { PlanetSelectorCallbackPipe } from './planet-selector-callback.pipe';

describe('PlanetSelectorCallbackPipe', () => {
  it('create an instance', () => {
    const pipe = new PlanetSelectorCallbackPipe();
    expect(pipe).toBeTruthy();
  });
});
