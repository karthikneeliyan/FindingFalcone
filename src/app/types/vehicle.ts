export class Vehicle{
    constructor(public name:string,public total_no:number,public max_distance:number,public speed:number){
        this.max_distance=max_distance;
        this.name=name;
        this.total_no=total_no;
        this.speed=speed;

    }
}